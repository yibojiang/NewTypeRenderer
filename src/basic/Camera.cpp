#include "Camera.h"

namespace new_type_renderer
{
    Camera::Camera()
    {
    }

    Vector3 Camera::GetCentriod() const
    {
        return Vector3{};
    }

    void Camera::UpdateTransformMatrix(const Matrix4x4&)
    {
    }

    void Camera::ComputeBounds()
    {
    }
}
